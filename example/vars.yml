repos:
  - name: sublime text 4
    apt:
      key: "https://download.sublimetext.com/sublimehq-pub.gpg"
      repo: deb https://download.sublimetext.com/ apt/stable/
      packages:
      - sublime-text
    yum:
      key: "https://download.sublimetext.com/sublimehq-rpm-pub.gpg"
      repo: "https://download.sublimetext.com/rpm/stable/x86_64"
      packages:
      - sublime-text
  
  - name: Table Plus
    apt:
      key: "http://deb.tableplus.com/apt.tableplus.com.gpg.key"
      repo: "deb [arch=amd64] https://deb.tableplus.com/debian tableplus main"
      packages:
      - tableplus
  

packages:
  apt:
  - curl
  - gron
  - python3
  - golang
  - htop
  yum:
  - httpd

snap:
  packages:
  - slack
  - postman
  classic:
  - code

go:
  install:
  - github.com/tomnomnom/gf@latest
  - github.com/projectdiscovery/nuclei/v2/cmd/nuclei@latest

pip:
  install:
  - django
  - fastapi[standard]
  - pandas
  - bs4

git:
  path: ${HOME}/ansible/local/
  clone:
  - name: ansible-practice-setup
    repo: https://github.com/Rishang/ansible-practice-setup.git
